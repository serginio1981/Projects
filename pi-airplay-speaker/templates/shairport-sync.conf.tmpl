// /etc/shairport-sync.conf — generado por pi-airplay-speaker.
// El original de la distribución queda respaldado en
// /etc/shairport-sync.conf.orig la primera vez.
// Las opciones no definidas aquí usan los valores por defecto de
// shairport-sync.
general = {
  name = "@AIRPLAY_NAME@";
};
